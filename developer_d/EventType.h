#ifndef EVENTTYPE_H
#define EVENTTYPE_H

// LLR29: Event tipleri enum
enum EventType {
    MOTION_DETECTED,
    SMOKE_DETECTED,
    GAS_DETECTED,
    DEVICE_FAILED,
    ALARM_TRIGGERED,
    STATE_CHANGED
};

#endif
